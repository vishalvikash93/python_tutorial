file_object = open('filename', 'mode')


# 'r': Read (default). Opens the file for reading.
# 'w': Write. Opens the file for writing (creates a new file if it doesn't exist, or truncates the file to zero length if it exists).
# 'a': Append. Opens the file for writing (creates a new file if it doesn't exist, or appends data to the end if it exists).
# 'b': Binary mode. Used to read/write binary files.
# 'x': Exclusive creation. Creates a new file, but fails if the file already exists.
# 't': Text mode (default). Used for text files.